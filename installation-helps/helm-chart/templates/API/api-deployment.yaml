kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: {{ .Release.Namespace }}
  name: estela-django-api
spec:
  selector:
    matchLabels:
      app: estela-django-api
  template:
    metadata:
      labels:
        app: estela-django-api
    spec:
      serviceAccountName: estela-api
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: estela-django-api
        image: {{ .Values.registryHost }}/estela-django-api
        imagePullPolicy: Always
        command: ['python', 'manage.py', 'runserver', '0.0.0.0:8000']
        envFrom:
        - secretRef:
            name: {{ .Values.secret }}
        - configMapRef:
            name: {{ .Values.config }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: {{ .Release.Namespace }}
  name: estela-celery-beat
spec:
  selector:
    matchLabels:
      app: estela-celery-beat
  template:
    metadata:
      labels:
        app: estela-celery-beat
    spec:
      serviceAccountName: estela-api
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: estela-celery-beat
        image: {{ .Values.registryHost }}/estela-celery-beat
        command: ['celery', '-A', 'config.celery', 'beat', '--loglevel=info']
        envFrom:
        - secretRef:
            name: {{ .Values.secret }}
        - configMapRef:
            name: {{ .Values.config }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: {{ .Release.Namespace }}
  name: estela-celery-worker
spec:
  selector:
    matchLabels:
      app: estela-celery-worker
  template:
    metadata:
      labels:
        app: estela-celery-worker
    spec:
      serviceAccountName: estela-api
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: estela-celery-worker
        image: {{ .Values.registryHost }}/estela-celery-worker
        command: ['celery', '-A', 'config.celery', 'worker', '--loglevel=info']
        envFrom:
        - secretRef:
            name: {{ .Values.secret }}
        - configMapRef:
            name: {{ .Values.config }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: {{ .Release.Namespace }}
  name: estela-redis
spec:
  selector:
    matchLabels:
      app: estela-redis
  template:
    metadata:
      labels:
        app: estela-redis
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: estela-redis
        image: {{ .Values.registryHost }}/estela-redis
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}

